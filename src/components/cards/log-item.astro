---
import { Dots } from "@components/misc";

const { title, description, slug, log_count } = Astro.props;
---

<a href={`/log/${slug}`}>
	<!-- sronly h3 -->
	<h3 class='sr-only'>{title}</h3>
	<p class='sr-only'>{description} log</p>
	<Dots log_count={log_count} />
</a>
