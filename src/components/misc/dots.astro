---
import { clsx } from "@utils/index";

const { log_count } = Astro.props;
---

<div
	class={clsx(
		'size-9 mb-4',
		log_count === 0 && 'bg-gray-100',
		log_count <= 2 && 'bg-[#D2FF50]',
		log_count > 2 && log_count <= 4 && 'bg-[#A0B91E]',
		log_count > 4 && 'bg-[#647D00]'
	)}>
</div>
